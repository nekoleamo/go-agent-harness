# 行尾纪律:Windows 检出默认会把文本转成 CRLF(仓库内均为 LF)。
# 「宿主真产物 == 桌面壳 fixture」这类逐字节比较的测试会因此假红 ——
# 2026-09-25 CI 实证:两侧打印内容完全一致、字符串比较却失败(唯一不可见差异是 \r)。
# 只钉住需要字节稳定的样板,不动其余文本(避免一次改动波及全仓)。
desktop/src-tauri/fixtures/*  text eol=lf
